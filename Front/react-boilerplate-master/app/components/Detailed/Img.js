import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
    position: relative;
    text-align: center;
    color: white;
    width: 100%;
`;

export default Img;
